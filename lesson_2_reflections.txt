What happens when you initialize a repository? Why do you need to do it?
	
	so that a folder named .git is made which will further record all the history of commits that are to be made in future.

How is the staging area different from the working directory and the repository?
What value do you think it offers?

	It is different as it allows us to commit some untracked files and neglects remaining untracked files.

How can you use the staging area to make sure you have one commit per logical change?

	I can use the staging area to add logical changes one by one and thus commiting them separately.

What are some situations when branches would be helpful in keeping your history organized? How would branches help?
	
	When we want to do add experimental feature or fix a bug or do another works at the same time without disturbing the original work, there comes the utilization of branches in git.

How do the diagrams help you visualize the branch structure?

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?
	
	Merging two branches results in a commit which contains the properties of both branches(unless there is a conflict).

What are the pros and cons of Gitâ€™s automatic merging vs. always doing merges manually?
